title: Design
type: page
theme: default

inherit:
    start:
        -'_all_start.yaml'
    end:
        -'_all_end.yaml'

regions:
    content:
        CustomHtml2:
            app: CustomHtml
            view: default
            controller: none
            parameters:
                title: 'Everything is an App'
                text: >
                      <p>Every content shown up on the website is from an App. Except few system critical functions, every other function is delegated to Apps. Apps are responsible for themselves.</p>
            subregions:
                title: title
                text: text
